{"eval_id":2,"eval_name":"consumer-dlq","prompt":"In my nuxt-nats app I have a durable pull consumer for 'orders.created' but it's not receiving anything in production even though the logs show NATS connected fine. It also needs to retry transient failures and dead-letter poison messages. What am I missing, and what should the consumer look like?","assertions":["Identifies NUXT_NATS_WORKERS gate (or server/plugins vs server/workers)","defineNatsConsumer in a server plugin","transient nak(delay) vs poison term; bare nak warning","NATS has no built-in DLQ; max_deliver = retry then stop","defineDeadLetterConsumer / advisory capture, not core sub"]}
