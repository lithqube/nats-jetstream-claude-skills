{"eval_id":1,"eval_name":"config-publish","prompt":"I'm adding NATS to my Nuxt 4 app using the nuxt-nats module. I need to connect to a 3-node production cluster with JWT+NKey auth (secrets come from env, not the repo), and publish an 'orders.created' event from a server API route with type safety and deduplication. Show me the nuxt.config setup and the publish code.","assertions":["Configures via nats: key in nuxt.config","Env-var injected auth (NUXT_NATS_USER_JWT + NUXT_NATS_NKEY_SEED)","jsPublish with msgId dedup","NatsEvents augmentation for typed subjects","Server-side publish (server/api)","All cluster nodes / provision:'never'"]}
